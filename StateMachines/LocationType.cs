namespace StateMachines
{
    /// <summary>
    /// Represents the different locations the miner can be at
    /// </summary>
    public enum LocationType
    {
        Shack,
        GoldMine,
        Bank,
        Saloon
    }
}
